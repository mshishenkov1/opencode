{
  "id": "TEST-DISPUTE-I14-07",
  "step": "I-14 / фикс-цикл по ревью review-i4-rev113-6-rev114-1, находка MAJ-C",
  "agent": "dev-agent",
  "test": "packages/app/src/corp/connectors-view.test.ts::AC-229: отсутствующее свойство строки не рисует и пустой строки не оставляет",
  "subject": "Мера записала СОСТАВ строки свойств («владелец и документация») литералами условий, и закрытие MAJ-C её красит",
  "claim": "Охраняемое правило верное и остаётся в силе: отсутствующее свойство строки не рисует, пустой полосы не остаётся, разделитель не висит у одинокого свойства, значение приходит внутрь `Show` аргументом (а не через `?? \"\"`). Но проверяется оно двумя литералами, которые кодируют не правило, а ЧИСЛО свойств: `<Show when={entry().owner || entry().docs_url}>` (охранник блока) и `<Show when={entry().owner && entry().docs_url}>` (единственный разделитель). Ревью того же цикла (MAJ-C, п.3) потребовало вернуть в строку третье свойство — способ подключения, — потому что макет заказчика от 30.08 показывает «Владелец — Платформа · Подключение — токен сессии ТЭГ · Документация — внутренний портал», а данные для него в карточке есть (`auth_methods`). С тремя свойствами оба литерала становятся ЛОЖНЫМИ утверждениями о правиле: охранник блока по двум полям спрятал бы строку у карточки, где известен только способ подключения, а единственный разделитель по `owner && docs_url` не тот, что нужен между вторым и третьим свойством. Мера красная не потому, что правило нарушено, а потому, что в ней записано число свойств.",
  "evidence": [
    "review-i4-rev113-6-rev114-1, MAJ-C: «в продукте — «Владелец — Платформа · Документация — https://…»: способ подключения из строки выпал, хотя данные для него в карточке есть (`auth_methods[0].title` = «Токен сессии ТЭГ»)», assignee включает dev.",
    "Макет 127.0.0.1:8900/connectors-mockups.html, экран 2, строка `m-note`: «Владелец — Платформа · Подключение — токен сессии ТЭГ · Документация — внутренний портал» — три свойства, а не два.",
    "Соседняя мера того же случая (AC-229, состав и порядок блоков) осталась ЗЕЛЁНОЙ: порядок `corp.connector.owner` → `corp.connector.docs` сохранён, третье свойство встало между ними.",
    "Продукт после правки: охранник блока — `<Show when={entry().owner || connectionMethodTitle(entry()) || entry().docs_url}>`, разделители — `entry().owner && connectionMethodTitle(entry())` и `(entry().owner || connectionMethodTitle(entry())) && entry().docs_url`; `<Show when={entry().owner}>` и `<Show when={entry().docs_url}>` на месте, `?? \"\"` не появилось."
  ],
  "proposed_resolution": "Перенацелить меру на правило, а не на число свойств: собрать перечень свойств строки из самого исходника (например, по `data-property=\"…\"` внутри среза `data-slot=\"corp-connector-properties\"`), и утверждать про КАЖДОЕ из них, что оно нарисовано под своим `Show` и получает значение аргументом, а разделителей ровно на один меньше, чем свойств, и каждый охраняется парой «есть что слева И есть что справа». Тогда четвёртое свойство расширит перебор само, а настоящий дефект (свойство без `Show`, висящий разделитель, `?? \"\"`, блок без охранника) мера ловить не перестанет.",
  "status": "resolved",
  "resolution": {
    "verdict": "accept — диспут принят",
    "resolved_by": "test-agent",
    "date": "2026-08-31",
    "summary": "Мера перенацелена ровно так, как предложено: перечень свойств строки собирается из самого исходника (по `data-property=\"…\"` внутри контейнера `data-slot=\"corp-connector-properties\"`), и правило утверждается про КАЖДОЕ найденное свойство. Литералов `entry().owner || entry().docs_url` и `entry().owner && entry().docs_url` в мере больше нет — числа свойств она не знает и от четвёртого свойства не сломается. Код продукта не менялся.",
    "what_the_measure_now_asserts": [
      "перечень свойств берётся из блока регулярным выражением по `data-property=\"…\"`, и он обязан совпасть с перечнем свойств, разобранных как дети-`Show` — свойство, нарисованное безусловно, в разбор не попадёт и обнаружится несовпадением;",
      "у каждого свойства значение приходит внутрь `Show` аргументом (`{(value) => …}` и `value()` в теле), а подстроки `?? \"\"` в блоке нет ни у одного;",
      "порядок детей блока — строго `свойство, разделитель, свойство, …`: разделителей ровно на один меньше, чем свойств, ни висящего с краю, ни двух подряд;",
      "охранник всего блока — ИЛИ по источникам ВСЕХ свойств строки (множества сравниваются, а не текст условия), и он стоит непосредственно перед контейнером строки;",
      "каждый разделитель условен парой «слева И справа»: справа — источник следующего свойства, слева — ИЛИ по источникам всех предыдущих."
    ],
    "injection_check": [
      "снят `Show` со свойства «способ подключения» (спан нарисован безусловно) → красная: перечень `data-property` не совпал с перечнем свойств под `Show`;",
      "значение владельца подставлено как `{entry().owner ?? \"\"}` вместо аргумента `Show` → красная: «owner: значение не приходит аргументом Show»;",
      "добавлен лишний разделитель перед документацией → красная: `property separator property separator separator property` против ожидаемого;",
      "дополнительно (мера не мертва на охранниках): из охранника блока убран источник способа подключения → красная; левая часть второго разделителя сужена до `entry().owner` → красная «разделитель 1: слева не все предыдущие».",
      "все инъекции откачены, `git status` по `packages/app/src/components/corp/` чист."
    ],
    "product_state": "Строка свойств продукта (три свойства: владелец, способ подключения, документация) новую меру проходит без единой правки кода: правило соблюдено, красной была именно запись числа свойств."
  }
}
