# Реестр правок upstream (S-B6)

Базовый тег upstream — `corp/upstream-base` (`v1.17.9`). Таблица перечисляет **все** файлы upstream,
изменённые в ветке `corp/i4-sso-connectors`, с правилом спецификации, причиной и коммитом.

Правило: файл upstream правится отдельным коммитом с заголовком `corp: <что и зачем>`; строка в этой
таблице добавляется тем же коммитом (S-C8).

| Файл upstream | Правило | Причина | Коммит |
|---|---|---|---|
| `bun.lock` | — | Пересчёт lock-файла после `bun install`: github-зависимость `ghostty-web` сдвинула коммит (`20bd361` → `83c0a07`). Вернуть строку upstream нельзя — с исходным пином `bun install --frozen-lockfile` падает («lockfile had changes»), а обычный `bun install` пересчитывает её обратно: ссылка на ветку в upstream-репозитории уехала, старый коммит больше не воспроизводится | `ff6671cf10` |
| `packages/opencode/src/config/config.ts` | S-C3, S-C4a, S-C9 | Мерж корпоративного слоя умолчаний до цикла `wellknown`, чтобы у слоя был наименьший приоритет; подмена адреса модели переменной `OPENCODE_CORP_MODEL_BASE_URL` внутри корп-слоя до слияния — иначе переменная перебивала бы пользовательские слои (BUG-I4-004); однократное предупреждение в лог о конфликтах личного конфига при старте (BUG-I4-006) | `06f3cada4e`, `6be9198cf6`, `047d9f04cd` |
| `packages/opencode/src/provider/provider.ts` | S-C4b, S-C9 | Проверка перед выдачей модели: без записи `magnit_prod` в auth-store запрос к LiteLLM не отправляется вовсе (BUG-I4-007), а выключенный личным конфигом провайдер называется по имени вместо upstream-ошибки «модель не найдена» (BUG-I4-006) | `0472a03633`, `047d9f04cd` |
| `packages/opencode/src/server/routes/instance/httpapi/middleware/error.ts` | S-C4b, S-C9 | Корп-ошибка обращения к модели отдаётся с причиной, а не как «Unexpected server error» (BUG-I4-007, BUG-I4-006) | `0472a03633`, `047d9f04cd` |
| `packages/opencode/src/cli/error.ts` | S-C4b, S-C9 | Текст причины, действия и подсказки `opencode corp login` в неинтерактивном режиме; код выхода 1 (BUG-I4-007, BUG-I4-006) | `0472a03633`, `047d9f04cd` |
| `packages/opencode/script/build.ts` | S-C2, S-I2, S-B2, S-B14 | Build-константы `OPENCODE_CORP_HUB_URL` и `OPENCODE_CORP_CONFIG` в блоке `define`; `VITE_OPENCODE_CORP_HUB_URL` для сборки встроенного веб-UI; отбор целей по `CORP_TARGETS`, чтобы сборка одной цели не тянула остальные. Ревизия 1.9 — константа `OPENCODE_CORP_CLI_UPDATE_URL` из `CORP_CLI_UPDATE_URL` (S-B14) | `06f3cada4e`, `5adcde570d`, `cdf62dc441`, `fad039f9d2` |
| `packages/opencode/src/server/routes/instance/httpapi/api.ts` | S-A1 | Регистрация группы корп-роутов `CorpApi` в `InstanceHttpApi` | `84e93ef0fe` |
| `packages/opencode/src/server/routes/instance/httpapi/server.ts` | S-A1 | Подключение `corpHandlers` к серверу | `84e93ef0fe` |
| `packages/sdk/openapi.json` | S-A1, S-V1 | Перегенерация OpenAPI-схемы после добавления корп-роутов; ревизия 1.9 — роут `DELETE /corp/connectors/{alias}` («Убрать из списка», S-V17), поля `error_class` в ответе `connect` и в карточке каталога (S-V19), поля `state` и `ever_connected` карточки и значение `forget` в наборе действий (S-V16) | `84e93ef0fe`, `f7a111cbaf` |
| `packages/sdk/js/src/v2/gen/sdk.gen.ts` | S-A1, S-V1 | Перегенерация SDK (`bun ./packages/sdk/js/script/build.ts`); ревизия 1.9 — метод `corp.forget()` роута `DELETE /corp/connectors/{alias}` | `84e93ef0fe`, `f7a111cbaf` |
| `packages/sdk/js/src/v2/gen/types.gen.ts` | S-A1, S-V1 | Перегенерация SDK; ревизия 1.9 — типы `CorpForgetResult`, `error_class` (ответ `connect` и карточка), `state`, `ever_connected` и действие `forget` | `84e93ef0fe`, `f7a111cbaf` |
| `packages/opencode/src/index.ts` | S-A12 | Регистрация группы команд `opencode corp` | `ee56c0514f` |
| `packages/tui/src/app.tsx` | S-T1, S-T2, S-T3 | Корп-команды `corp.connectors` / `corp.login` и ветвление first-run на корп-экран входа | `1b32c4d283` |
| `packages/tui/src/config/keybind.ts` | S-T1, S-T6, S-T10 | Имена корп-команд и действий витрины (без них действия не отображаются); ревизия 1.9 — клавиша `x` действия «Убрать из списка» (S-V17); ревизия 1.10 — клавиша `f` вкладок фильтра (S-V11) и переезд «Подключить» с `return` на `c`: `return` открывает страницу коннектора (S-D11), и две команды на одной клавише разошлись бы поведением | `1b32c4d283`, `70baa8be1c`, `7eac13044d` |
| `packages/tui/src/ui/dialog-select.tsx` | S-T10, S-D11 | Необязательный пропс `initialFilter` — начальное значение строки поиска. Возврат со страницы коннектора обязан вернуть «ту же вкладку, ту же строку поиска и ту же выделенную строку» (S-T10), но в терминале страница открывается `dialog.replace` — тем же правилом, — поэтому витрина пересоздаётся, а способ Desktop («сохранённое состояние живого компонента», S-D11) здесь неприменим. Поле поиска диалога неуправляемое: `value` ему не передавался, а `store.filter` наружу отдан только геттером `DialogSelectRef.filter`, то есть восстановить запрос было нечем. Правка — три строки: пропс в интерфейсе, `filter: props.initialFilter ?? ""` в начальном состоянии и присваивание `input.value` в существующем обработчике `ref`, под проверкой непустоты. Пропсом `value` это делать нельзя: пропсы применяются присваиванием (`node[name] = value`), а сеттер `value` у `InputRenderable` зовёт `value.substring(...)`, то есть `undefined` уронил бы каждый диалог, который `initialFilter` не передаёт. Значение читается один раз при создании, дальше строкой владеет само поле; без пропса поведение диалога совпадает с upstream, поэтому прочие вызовы `DialogSelect` не затронуты | `b9d161300c`, `cf64830a92` |
| `packages/tui/src/component/dialog-provider.tsx` | S-T5 | Перехват выбора провайдера `magnit_prod` — открывается корп-экран входа | `1b32c4d283` |
| `packages/tui/src/component/dialog-mcp.tsx` | S-V13 | Подпись `title` из кэша каталога рядом с alias | `1b32c4d283` |
| `packages/app/src/pages/layout.tsx` | S-D1, S-D3, S-D4, S-D2b, S-D4a | Команды `corp.connectors` / `corp.login`, корп-кнопка в блоке Getting Started; корп-пропсы рельсы `SidebarContent` (только при `enabled=true`) и однократное автооткрытие экрана входа при первом запуске | `5adcde570d`, `0e30889d64`, `a1aa05ed89` |
| `packages/app/src/pages/home.tsx` | S-D2a | Строка «Коннекторы» первой в нижнем блоке боковой панели Home, над «Настройками»; видимость — по `enabled` из `GET /corp/status` | `0e30889d64` |
| `packages/app/src/pages/layout/sidebar-shell.tsx` | S-D2b | Необязательные пропсы `connectorsLabel`/`onOpenConnectors` и кнопка коннекторов над шестерёнкой: без пропсов рельса совпадает с upstream | `0e30889d64` |
| `packages/ui/src/v2/components/icon.tsx` | S-D2a | Глиф `mcp` в наборе v2: подходящего имени в наборе нет, а `IconV2` молча подменяет неизвестное имя на `plus` — пункт «Коннекторы» рисовал бы плюс (D-17) | `127ea0ad68` |
| `packages/app/src/components/dialog-select-provider.tsx` | S-D5 | Перехват выбора провайдера `magnit_prod` | `5adcde570d` |
| `packages/app/src/context/language.tsx` | S-I2 | Русский язык по умолчанию при включённых корп-функциях | `5adcde570d` |
| `packages/app/vite.js` | S-B3, S-B10 | Единственный источник канала для интерфейса и для главного процесса: экспортируемый `resolveChannel()` знает `magnit` (в него же переводится `latest`) и бросает ошибку на неизвестном значении. Прежде здесь была вторая, расходящаяся копия разбора, молча сваливавшаяся в `dev`: главный процесс знал канал `magnit`, а интерфейс — нет, и рисовал в заголовке окна корпоративной сборки ярлык «DEV» | `bda0f4a9a2` |
| `packages/app/src/context/settings.tsx` | S-B3, S-B10 | `newLayoutDesignsDefault` считает `magnit` каналом раздачи наравне с `prod`: корпоративная сборка — раздача, а не preview, и непоказанные макеты в ней по умолчанию выключены. Пока канал не доезжал до интерфейса, сборка выглядела для него как `dev` и включала их сама собой | `bda0f4a9a2` |
| `packages/app/src/env.d.ts` | S-C2, S-I2, S-B10 | Объявление `VITE_OPENCODE_CORP_HUB_URL` для веб-UI; значение `magnit` в типе `VITE_OPENCODE_CHANNEL` (`bda0f4a9a2`) | `5adcde570d` |
| `packages/app/src/i18n/en.ts` | S-I1 | Ключи `corp.*`; ревизия 1.9 — подписи состояний карточки (`connected`, `retry`, `forget`, `forgetConfirm`, `forgetHubFailed`, `neverConnected`, `lost`, `disconnected`), объяснения классов ошибок подключения `corp.error.connect.*` и сообщение отключённого апгрейда CLI `corp.upgrade.disabled` | `5adcde570d`, `b99530b660` |
| `packages/app/src/i18n/ru.ts` | S-I1 | Ключи `corp.*`; ревизия 1.9 — те же ключи, что в `en.ts` (паритет словарей S-I3) | `5adcde570d`, `b99530b660` |
| `packages/desktop/electron-builder.config.ts` | S-B9, S-B10, S-B11, S-B13 | Канал `magnit` в разборе `OPENCODE_CHANNEL` и в `APP_IDS`, ветка `case "magnit"` (`appId` `ai.opencode.desktop.magnit`, `productName` «OpenCode Magnit», имя артефактов, `rpm.packageName`), ошибка вместо молчаливого отката в `dev` и блок `publish` из `CORP_DESKTOP_UPDATE_URL`. Без своей идентичности корпоративная сборка делила каталог данных с ванильным OpenCode, а её апдейтер тянул релизы `anomalyco/opencode` (ревизия 1.6). Ветки `dev`/`beta`/`prod` и `getBase()` не изменены. Ревизия 1.9 (S-B9 п. «д») — `extraMetadata.name` = `opencode-magnit-desktop` в ветке `magnit`: каталог кеша апдейтера выводится из поля `name` пакета (F42), и с upstream-значением корпоративная сборка искала обновление в общем с ванильной каталоге `@opencode-aidesktop-updater` | `bfc3ce9b1b` |
| `packages/desktop/electron.vite.config.ts` | S-B10, S-B11 | Канал берётся из общего `resolveChannel()` пакета `app` — того же места, откуда его берёт `define` интерфейса, чтобы источник канала был один (`bda0f4a9a2`); прежде здесь была своя копия разбора. Build-константа `OPENCODE_CORP_UPDATE_URL` из `CORP_DESKTOP_UPDATE_URL` для главного процесса | `bfc3ce9b1b` |
| `packages/desktop/scripts/utils.ts` | S-B10 | `resolveChannel` знает канал `magnit`, на неизвестном значении бросает ошибку с полученным значением и списком допустимых | `bfc3ce9b1b` |
| `packages/desktop/scripts/copy-icons.ts` | S-B10 | Неизвестный аргумент канала — ошибка; иконки берутся из `icons/<канал>`, а при отсутствии каталога — из `icons/prod`: своего набора у `magnit` пока нет, но сборка обязана проходить | `bfc3ce9b1b` |
| `packages/desktop/src/main/constants.ts` | S-B10, S-B11, S-B15 | Тип канала с `magnit` и ошибка на неизвестном значении; константа `UPDATE_URL` и `UPDATER_ENABLED = app.isPackaged && CHANNEL !== "dev" && !!UPDATE_URL` — без адреса внутреннего сервера автообновление выключено целиком. Ревизия 1.9 — таблица имён сборок `APP_NAMES` и функция `appName()` переехали сюда, к каналу: их читают и `app.setName`, и заголовок окна, а второй копии заводить нельзя (D-33) | `bfc3ce9b1b`, `6f2943b469` |
| `packages/desktop/src/main/env.d.ts` | S-B11 | Объявление build-константы `OPENCODE_CORP_UPDATE_URL` для главного процесса | `bfc3ce9b1b` |
| `packages/desktop/src/main/index.ts` | S-B10, S-B12, S-B15 | `APP_NAMES.magnit` = «OpenCode Magnit» и `APP_IDS.magnit` = `ai.opencode.desktop.magnit`: каталог пользовательских данных выводится из `appId`, поэтому свой идентификатор даёт свои настройки, свой `electron-store` и свою блокировку единственного экземпляра рядом с ванильной сборкой. Ревизия 1.9 — таблица `APP_NAMES` вынесена в `./constants`, `app.setName` зовёт общий `appName()` | `bfc3ce9b1b`, `6f2943b469` |
| `packages/desktop/src/main/windows.ts` | S-B15 | Заголовок окна — `appName()` вместо литерала `title: "OpenCode"` (F44), одинакового у всех каналов: корпоративная сборка донашивала заголовок ванильной. Источник имени и канала общий с `app.setName` — второй копии разбора канала и второй таблицы имён нет (D-33) | `6f2943b469` |
| `packages/desktop/src/main/updater.ts` | S-B11 | При выключенном апдейтере обработчики не регистрируются, `autoUpdater` не трогается, в лог идёт одна строка `auto updater disabled` с причиной, а унаследованная запись `ready` очищается (наблюдался скачанный ванильный 1.18.19 в состоянии `ready`); при включённом — канал фида `magnit` и адрес фида в логе; пункт проверки обновлений отвечает диалогом об отключённом автообновлении | `bfc3ce9b1b` |
| `packages/desktop/src/main/migrate.ts` | S-B10, S-B12 | `TAURI_APP_IDS.magnit` = `ai.opencode.desktop.magnit`: предшественника на Tauri у канала нет, каталога не найдётся и миграция станет пустой операцией. Без строки индекс по `CHANNEL` давал `undefined`, и `join()` ронял приложение на старте; ванильный `ai.opencode.desktop` здесь брать нельзя — он затянул бы данные ванильной сборки в корпоративную | `bfc3ce9b1b` |
| `packages/desktop/src/main/menu.ts` | S-B11 | Пункт «Check for Updates» остаётся доступным при выключенном апдейтере — иначе диалог «Automatic updates are disabled in this build.» недостижим (серый пункт не нажимается) | `bfc3ce9b1b` |
| `packages/opencode/script/build-node.ts` | S-C2, S-B1, S-B3, S-B14 | Build-константы `OPENCODE_CORP_HUB_URL` / `OPENCODE_CORP_CONFIG` и `OPENCODE_VERSION` для сервера, встроенного в Desktop: без них корп-режим в приложении был выключен, хотя CLI из той же сборки корпоративный (BUG-I4-003). Ревизия 1.9 — константа `OPENCODE_CORP_CLI_UPDATE_URL` (S-B14) | `e8cea036a4`, `fad039f9d2` |
| `packages/opencode/src/cli/cmd/upgrade.ts` | S-B14 | Явный `opencode upgrade` в корпоративной сборке: без `CORP_CLI_UPDATE_URL` — ни одного сетевого запроса, сообщение `corp.upgrade.disabled` и код выхода 1; с адресом — версия из `GET <адрес>/latest` и артефакт с того же хоста, без `api.github.com` и публичных реестров (F43). Ванильная сборка идёт прежним путём: `plan()` отдаёт `upstream` (S-C6) | `fad039f9d2` |
| `packages/opencode/src/cli/upgrade.ts` | S-B14 | Фоновая проверка подчиняется тем же правилам, что явная команда: корп-умолчание `autoupdate:false` её выключает, но личный конфиг сильнее корп-слоя (S-C3/S-C9) — без адреса проверка не выполняется вовсе и один раз пишет причину в лог, с адресом ходит только на него | `fad039f9d2` |

Новые файлы, не относящиеся к upstream (в таблице не перечисляются): `corp/**`,
`packages/core/src/corp/**`, `packages/opencode/src/corp/**`, `packages/opencode/src/cli/cmd/corp.ts`,
`packages/opencode/src/server/routes/instance/httpapi/{groups,handlers}/corp.ts`,
`packages/tui/src/corp/**`, `packages/tui/src/component/corp/**`, `packages/app/src/corp/**`,
`packages/app/src/components/corp/**`, `packages/app/src/context/corp.ts`,
`.github/workflows/corp-ci.yml`, `.github/workflows/corp-release.yml`,
`.github/workflows/upstream-sync.yml`.

Ревизия 1.10 (S-B16): цепочка релиза целиком собрана из корп-файлов — автотег
(`corp-release.yml`), сбор фида и публикация (`corp/build.ts`, `corp/verify-desktop-bundle.ts`,
`corp-ci.yml`). Правок upstream-файлов в ней нет, поэтому новых строк в таблице выше не появилось.
